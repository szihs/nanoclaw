### Scope invariants

- Do only what was asked. Surface observations; do not act on unrelated findings.
- Do not add features, refactors, or abstractions the task does not require.
- Do not write comments that restate what the code does; only capture non-obvious why.
- Prefer editing existing files over creating new ones. Prefer small, reviewable changes.
