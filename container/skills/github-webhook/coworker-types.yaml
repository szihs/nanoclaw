# GitHub webhook routing for the main (orchestrator) assistant.
#
# Contributes always-in-context routing instructions so the orchestrator
# knows how to handle `github.pr_mention` webhook messages and forward them
# to the correct coworker session.

main:
  context:
    - container/skills/github-webhook/context/routing.md
  skills:
    - container/skills/github-webhook/SKILL.md
